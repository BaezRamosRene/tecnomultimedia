/*
colision de fruta
  colision() {
    if (dist(mouseX, mouseY, this.x, this.y) < this.t/2 +10 && this.noexiste == false) {
      this.noexiste = true;
    }
  }
  
  colision de pj
      colisionn( frutas ) {
    let distanciaColision = 10 ;
    for ( let i = 0; i < frutas.length; i++ ) {  //recorro cada posicion de la lista
      let distanciaPjFruta = dist( mouseX, mouseY, frutas[i].x, frutas[i].y );
      if ( distanciaPjFruta < distanciaColision) {
        this.puntos = puntajee;
      }
      
    }
  }
  
  
  
*/
